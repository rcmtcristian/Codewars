// Return the type of the sum of the two arguments

// FUNDAMENTALS

// Main solution
function typeOfSum(a, b) {
  return typeof (a + b);
}

// Other solutions
const typeOfSum = (a, b) => typeof (a + b);
