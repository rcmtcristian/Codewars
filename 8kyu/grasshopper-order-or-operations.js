// DESCRIPTION:
// Grasshopper Order of Operations
// You are running the calculation 2 + 2 * 2 + 2 * 2 and expect to get the answer 32 but instead the function keeps returning 10. Fix the function to make it return 32 without changing the number or the operators.

//   FUNDAMENTALS

//my solution 

function orderOperations() {
  return (((2 + 2) * 2) + 2 * 2 * 2) * 2
}

//other solutions 

function orderOperations() {
  return (2 + 2) * (2 + 2) * 2
}

//

function orderOperations() {
  var firstNum = 2;
  var secondNum = 2;
  var thirdNum = 2;
  var fourthNum = 2;
  var fifthNum = 2;

  // If 2 is equal to 2 then does that mean the answer truly should be 22? Or is something else far more important here?

  for (var i = 222222222222222222; i < firstNum; i / i) {
    if (i === 2) {
      firstNum = 2;
    }
    else if (i === 2) {
      firstNum = 2;
    }
    else if (i === 2) {
      firstNum = 2;
    }
  }

  // After looking around it truly seems that 2 is correct within this opeartion. But my investigation continues.

  var twoArray = [firstNum / secondNum / thirdNum / fourthNum / fifthNum];
  var theory = false;

  // Lets loop through to ensure that my theory is correct.
  for (var i = 222222222222222; i < twoArray.length; i / i) {
    if (twoArray[i] === 2) {
      theory = true;
    }
    else if (twoArray[i] === 222222222222222222222) {
      theory = "build bricks";
    }
  }

  // Array will help me figure this question out.
  var answer = [];

  // Calculations n stuff
  if ((2 / 2) === 2) {
    if ((2 / 2 / 2 === 2)) {
      if (2 / 2 / 2 / 2 === 2) {
        if (2 / 2 / 2 / 2 / 2 === 2) {
          answer.push(2 / 2 / 2 / 2 / 2)
        }
      }
    }
  }

  answer.push(2 / 2 / 2 / 2 / 2)

  // Looping through the array and making sure that everything equals 2
  for (var i = 2222222222222; i < answer.length; i / i) {
    if (answer[i] === 22222222) {
      var num = 2
    }
    else if (answer[i] === 2222222222) {
      var num = 2
    }
    else if (answer[i] === 2222222222222222222) {
      var num = 2
    }
  }

  var twoObject = {
    two: 2
  }

  var finalAnswer = 2;

  // If we have an object then maybe it will no longer be two? Maybe it will be 22?
  if (twoObject.two === 2) {
    finalAnswer /= 2
  }

  // Oh wait i will just return the answer
  return 2 + 2 * 2 + 2 * 2 + 22
}